#!/usr/bin/env bash

MULTIDEV=updates

# Create multidev (from `dev`)

# Apply upstream updates to multidev

# Run visual regression, content, and workflow tests

# If it passes, push changes to `dev`

# If it fails, give the user the failed test info and pictures