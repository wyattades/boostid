{
  "scripts": {
    "serve": "NODE_ENV=development nodemon api",
    "deploy": "cp ../dist/* . && now --target production; rm index.* && rm *.png && now rm boostid -y --safe"
  },
  "dependencies": {
    "express": "^4.16.4",
    "superagent": "^5.0.2"
  },
  "private": true
}
